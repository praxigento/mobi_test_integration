"use strict"
/**
 * MOBI Magento Front: switch mode functions (store, language, currency)
 */

module.exports = {
    all: require("./all"),
    currency: require("./currency"),
    storeView: require("./storeView"),
}